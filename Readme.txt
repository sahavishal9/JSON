Write a Python Program which should perform following actions.

1. Read the file using console arguments.

2. Check if the path is valid and file contains the valid json.

3. If data is valid perform following tasks on the data dictionary.
  
  3.a If the type of value in dictionary is a string : reverse the string.
  
  3.b. If the type of value in dictionary is a number : square the number.
    
  3.c. If the type of vlue in dictionary is a List. : perform above two operations
       based on the type of the data in the list.
       
4. Create log-file to store all errors and relevant data of the program.
